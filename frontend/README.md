# Family Planner Frontend

React + Vite + TailwindCSS frontend for the Family Planner MVP.

## Scripts

```powershell
npm.cmd run dev
npm.cmd run build
npm.cmd run lint
```

The frontend expects the API at `http://localhost:5123` by default. Override
`VITE_API_BASE_URL` if a different backend URL is needed.
